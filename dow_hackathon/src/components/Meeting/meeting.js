import React, {Fragment} from 'react';

require('./style.css');

const Stream = (props) => {
    return (
        <Fragment>
            <h1> Meeting Planner </h1>
        </Fragment>
    )
};

export default Stream;